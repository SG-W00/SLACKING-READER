# 🖱️ 滚动功能说明

## 已实现的功能

### 1. 鼠标滚轮滚动
- 将鼠标移到阅读器内容区域
- 滚动鼠标滚轮即可上下滚动内容
- 滚动流畅，带有平滑效果

### 2. 自动跳转下一章
- 滚动到章节底部时，自动跳转到下一章
- 无需手动点击"下一章"按钮
- 更加流畅的阅读体验

### 3. 快捷键
- `←` 键：上一章
- `→` 键：下一章
- `ESC` 键：关闭设置面板

### 4. 点击按钮翻页
- 工具栏的 `<` 和 `>` 按钮
- 上一章/下一章快速切换

## 使用方法

### 正常阅读
1. 导入小说后，内容会自动显示
2. 使用鼠标滚轮滚动阅读
3. 滚到底部自动跳下一章

### 快速跳转
- 点击 `<` 或 `>` 按钮直接跳转章节
- 使用左右方向键快速翻页

### 调整字体
- 打开设置面板（点击 `≡` 按钮）
- 调整字体大小以获得最佳阅读体验

## 优化建议

### 舒适阅读
- 字体大小：13-14px
- 行高：1.5-1.6
- 窗口大小：适中，方便滚动

### 隐蔽模式
- 窗口透明度：85%-90%
- 内容透明度：90%-95%
- 字体不要太大，避免引起注意

## 技术细节

- 使用 `overflow-y: auto` 实现垂直滚动
- `scroll-behavior: smooth` 平滑滚动效果
- 监听 `wheel` 和 `scroll` 事件
- 底部留白 100px 便于滚动
- 滚动检测延迟 100ms 避免频繁触发
